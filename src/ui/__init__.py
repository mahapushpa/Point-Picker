"""ui — PySide6 (Qt) layer only. NO domain logic lives here.

This package is a thin renderer that calls into core/ and io/. Any real logic
that appears here is a defect to move into core/io immediately. Populated from
Milestone 2 onward (native QGraphicsView canvas, dialogs, forms).
"""
