"""polygon — point / path / closed-polygon data model (incl. optional lat/lon).

Not implemented yet (Milestone 4). Ports the point/label data model from the
point_picker.html logic into plain Python. Pure Python, no UI imports.
"""
