"""core — pure-Python domain logic. No PySide6 / UI-framework imports allowed.

Everything in this package must be importable and testable with no GUI running,
so a Phase 2 UI rewrite is UI-only. Implemented so far: project_db (Milestone 1).
"""
