"""Land Parcel Measurement & Revenue Records Tool — application package.

Imported as the top-level package ``src`` so that the sub-package ``src.io``
does not collide with Python's standard-library ``io`` module (a bare top-level
``io`` on sys.path is shadowed by the stdlib and is not importable). Run the app
with ``python -m src.main`` from the repository root.
"""
